<nav class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container">
	<div class="row">
	  <div class="col-md-10"><h4>Light IT test task :)</h4></div>
	  <div class="col-md-2">
	  	<% if user_signed_in? %>
  			<%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-info navbar-btn" %>
  		<% else %>
  			<%= link_to "Messages page", posts_path, class: "btn btn-info navbar-btn " %>
  		<% end %>
  	  </div>
	  </div>
  </div>
</nav>